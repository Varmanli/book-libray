export type ExplicitFieldAction = "KEEP_EXISTING" | "FILL_IF_EMPTY" | "USE_SOURCE" | "USE_CUSTOM";
export function isSemanticallyEmpty(value: unknown) { return value == null || (typeof value === "string" && !value.trim()); }
/** Returns undefined for a no-op. Callers must update only defined fields. */
export function resolveFieldAction<T>(input: { action: ExplicitFieldAction; current: T; source: T; custom?: T }) { if (input.action === "KEEP_EXISTING") return undefined; if (input.action === "FILL_IF_EMPTY") return isSemanticallyEmpty(input.current) && !isSemanticallyEmpty(input.source) ? input.source : undefined; const next = input.action === "USE_SOURCE" ? input.source : input.custom; return next === undefined || Object.is(next, input.current) ? undefined : next; }
export function applyExplicitFields<T extends Record<string, unknown>>(current: T, actions: Array<{ field: keyof T & string; action: ExplicitFieldAction; customValue?: unknown }>, source: Partial<T>) { const patch: Partial<T> = {}; for (const item of actions) { const value = resolveFieldAction<unknown>({ action: item.action, current: current[item.field], source: source[item.field], custom: item.customValue }); if (value !== undefined) (patch as Record<string, unknown>)[item.field] = value; } return patch; }
